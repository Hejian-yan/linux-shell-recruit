# Task 02 Answer

1.为什么 ./tools/recruit-info 可以运行，而直接输入 recruit-info 找不到它？

./tools/recruit-info 使用相对路径，shell直接根据路径定位文件，只要文件有执行权限就运行。 直接输入 recruit-info 不带路径时，shell不会在当前目录查找，只会去PATH环境变量记录的目录搜索，tools目录不在PATH里

2.当我们输入 ls 时，Shell 是如何找到对应程序的？

shell在PATH里递归遍历每一个目录，直到找到ls文件并运行

